<%

presentation ||= Presentation.new

%>

<%= render 'shared/card', title: presentation.name, project: presentation.project, subtitle: "#{presentation.width_px}x#{presentation.height_px}", description: presentation.description, render_actions: 'presentations/shared/card-actions', render_actions_args: { presentation: presentation } do %>

<%= render 'presentations/shared/card-content', presentation: presentation %>

<% end %>