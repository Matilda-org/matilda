<%

procedure ||= Procedure.new

%>

<%= link_to I18n.t('app.labels.details'), request.params.merge(project_id: procedure.project_id, procedure_id: procedure.id), class: 'card-link', data: { turbo_frame: '_top' } %>
