<%= turbo_frame_tag 'form_vectorsearch' do %>

  <div class="alert alert-secondary" role="alert">
    L'assistente virtuale fornisce, tramite chat, le seguenti informazioni:
    <ul>
      <li>Informazione generiche sui progetti registrati.</li>
      <li>Informazioni generiche sui task registrati.</li>
      <li>Creazione automatica dei task su richiesta.</li>
    </ul>
    Inoltre, l'assistente virtuale fornisce una serie di micro-funzionalità per facilitare le operazioni nella piattaforma.
  </div>

  <%= form_tag settings_edit_vectorsearch_action_path, class: 'd-flex flex-column', data: { turbo_frame: '_self', controller: 'form-loading' } do %>
    <div class="col col-12 mb-3">
      <label for="vectorsearch_openai_key" class="form-label">Chiave api OpenAI</label>
      <input type="password" class="form-control" id="vectorsearch_openai_key" name="vectorsearch_openai_key" value="<%= Setting.get('vectorsearch_openai_key') %>">
    </div>
    
    <div class="d-flex justify-content-end">
      <a href="<%= settings_reset_action_path(key: 'vectorsearch') %>" data-turbo-method="post" data-turbo-confirm="Sei sicuro di procedere?" class="btn btn-link">Reset</a>
      <button type="submit" class="btn btn-primary" data-form-loading-target="submit">Conferma</button>
    </div>
  <% end %>

<% end %>